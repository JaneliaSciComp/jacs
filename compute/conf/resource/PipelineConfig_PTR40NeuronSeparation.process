<process name="PTR 40 Neuron Separation" processor="java:/jms/queue/samplePipelineLauncher" startEvent="pending" updateProcessStatus="on_success">
    <sequence>
    
        <include name="Pipeline" process="FlyLightPairedSamplePipeline">
            <input name="PIPELINE_PROCESS" value="FlyLightPairedSample"/>

            <!-- use default JBA and ANTS 20x alignment -->

            <!-- use default 20x neuron separation -->

            <!-- use default ANTS 63x alignment -->

            <!-- use default 63x neuron separation -->

        </include>

    </sequence>
</process>