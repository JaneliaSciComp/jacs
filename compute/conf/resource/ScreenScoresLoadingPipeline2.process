<!-- 
	Load in the additional marks with screen pattern intensity scores.
-->
<process name="Screen Score Loading Pipeline" processor="queue/mcfoDataPipelineLauncher" startEvent="pending">
	<sequence>
	
	    <!-- Load everything and create entities as needed -->
		<operation name="Load Screen Scores" processor="org.janelia.it.jacs.compute.service.fly.ScreenScoresLoadingService2">
		</operation>
		
    </sequence>
</process>